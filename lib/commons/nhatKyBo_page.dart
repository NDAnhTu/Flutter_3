import 'package:flutter/material.dart';

class NhatKyBo_Page extends StatelessWidget {
  const NhatKyBo_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('NhatKiBo'),
      ),
    );
  }
}
